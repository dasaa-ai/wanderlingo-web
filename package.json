{
  "name": "wanderlingo-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --port 3000 --strictPort",
    "preview:ci": "http-server ./dist -p 3000 -s",
    "e2e": "playwright test"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "http-server": "^14.1.1",
    "wait-on": "^7.0.1"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.8"
  }
}
